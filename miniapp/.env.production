# 生产构建（uni build -p mp-weixin / build:h5）自动加载本文件，覆盖 .env。
# 小程序端必须用绝对 HTTPS 地址，且该域名要在微信公众平台「开发设置-服务器域名」
# 的 request 合法域名里登记（须已备案 + 已配 TLS 证书）。
VITE_API_BASE=https://youyuji.com/api

# 记账提醒的微信一次性订阅消息模板 id（公众平台「订阅消息」创建后填入）。
# 与后端 app.wechat.subscribe.reminder-template-id 必须是同一个；留空则提醒授权不可用。
VITE_WX_REMINDER_TMPL_ID=

# 复制为 .env 并按环境填写。Vite 只暴露以 VITE_ 开头的变量。
# 小程序端务必在微信公众平台「开发设置-服务器域名」把该域名加入 request 合法域名。
VITE_API_BASE=https://your-domain.com/api

# 记账提醒的微信一次性订阅消息模板 id（微信公众平台「订阅消息」处创建后填入）。
# 与后端 app.wechat.subscribe.reminder-template-id 必须是同一模板 id。
VITE_WX_REMINDER_TMPL_ID=

# 预算提醒的微信一次性订阅消息模板 id（独立于记账提醒的另一个模板）。
# 与后端 app.wechat.subscribe.budget-template-id 必须是同一模板 id。
VITE_WX_BUDGET_TMPL_ID=


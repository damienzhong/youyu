org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
org.gradle.parallel=true
org.gradle.caching=true
# 本工程刻意不依赖任何 androidx/第三方库（纯 framework API），
# 以便在受限环境（如 QEMU 模拟的 amd64 容器）下也能快速构建。
